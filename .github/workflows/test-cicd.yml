name: CI/CD - Child 01

on:  
  push:
    branches: [ "main" ]
  
  workflow_dispatch:

jobs:  
  build:    
    runs-on: ubuntu-latest
    
    steps:      
      - uses: actions/checkout@v3
      
      - name: Print
        run: |
          echo Execute child 01 ci/cd pipeline.
          echo Testing child 01 ci/cd pipeline.
